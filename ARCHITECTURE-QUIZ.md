# 딥페이크 판별 퀴즈 코드 분석

분석 대상: 클로드 채팅으로 만든 단일 HTML 파일 `딥페이크 판별 퀴즈 — 진짜일까, 가짜일까?`
(437줄 · HTML + CSS + 바닐라 JS 한 덩어리)

이 문서는 원본이 **어떻게 작동하고 어떻게 구성되어 있는지**를 정리한 것입니다.
이 저장소의 `src/quiz/`는 아래 구조를 React로 옮긴 이식본입니다.

---

## 1. 한 줄 요약

사건 파일 8건을 하나씩 보여 주고 → 학생이 판단 근거를 적고 → 진짜/딥페이크를 고르면 →
결정적 단서에 형광펜이 켜지면서 해설과 **반 전체 정답률**이 나오는, **한 장짜리 퀴즈 앱**입니다.

이 앱이 실제로 하는 계산은 두 가지뿐입니다. **맞았는지 비교**(`picked === real`)와
**정답률 비율**(`correct / total`). 나머지는 전부 화면 조립입니다.
그래서 이 앱의 값어치는 코드가 아니라 **8개 사건의 단서 설계**에 있습니다.

## 2. 원본의 구성

파일 하나 안에 세 덩어리가 순서대로 들어 있습니다.

| 구간 | 줄 | 내용 |
| --- | --- | --- |
| `<style>` | 7~155 | CSS 변수 팔레트, 사건 카드, 단서 목록, 해설 상자, 결과 화면 |
| `<body>` | 157~177 | 헤더 · 진행 막대 · **빈 `<div id="card">`** · 푸터 |
| `<script>` | 179~435 | 문항 데이터 8건, 집계 저장소, 렌더·채점·결과 |

프레임워크도 빌드 도구도 없습니다. 화면 전체가 `#card` 하나이고,
문항이 바뀔 때마다 그 안을 `innerHTML`로 통째로 다시 씁니다.

```js
let idx = 0, score = 0, answered = false;
const reasons = {};   // idx -> 학생이 적은 근거
```

## 3. 문항 데이터 — 이 앱의 본체

`Q` 배열(181~269줄)의 원소 하나가 사건 파일 한 장입니다.

```js
{ kind:"📺", src:"YTN 속보", time:"오늘 09:14", live:true,   // 파일 머리줄
  headline:"…", situation:`… <b>강조</b> …`,                 // 상황 (HTML 문자열)
  observe:[ {t:"…", flag:true}, … ],                         // 관찰 단서 3개
  q:"이 ‘속보’ 영상, 진짜일까요?", real:false,               // 질문과 정답
  why:"… <b>강조</b> …" }                                    // 해설 (HTML 문자열)
```

핵심은 `flag`입니다. **답을 고르기 전에는 단서 세 줄이 모두 같은 무게로 보이고**,
고른 뒤에 `.observe.revealed` 클래스가 붙으면서 `flag:true`인 줄에만 노란 형광펜이 켜집니다.
"무엇을 봤어야 했는지"를 뒤늦게 알려 주는 이 연출이 이 퀴즈의 교육적 장치입니다.

정답이 '진짜'인 문항(3·5번)은 `flag`가 하나도 없어서, 해설에 "이상 신호가 없다"는 문장이 대신 나옵니다.

## 4. 엔진 — 함수 네 개

```
render()  →  choose(btn)  →  next()  →  render() … → result()
```

- **`render()`** (302~336줄) — `#card`의 `innerHTML`을 새로 쓰고, 근거 입력칸과 버튼 두 개에 리스너를 답니다.
- **`choose(btn)`** (338~378줄) — 채점(`picked === it.real`), 점수 +10, 근거 잠금(`readonly`),
  단서 공개(`revealed`), 집계 갱신, 해설 상자 그리기까지 한 함수가 다 합니다.
- **`next()`** (380줄) — 다음 문항이거나 결과.
- **`result()`** (382~430줄) — 등급 판정, 문항별 반 정답률 목록, 판별 요령 5줄.

상태 갱신과 화면 조립이 한 덩어리라, 어느 줄이 무엇을 바꾸는지 읽어야만 알 수 있습니다.
React로 옮기면 `render()`는 통째로 사라지고 `choose()`는 채점만 남습니다.

## 5. 반 정답률이 저장되는 곳

```js
const TALLY_KEY = "deepfake_quiz_tally_v1";
const hasStore = (typeof window !== "undefined" && window.storage);
let localTally = {};
```

`window.storage`는 클로드 아티팩트 런타임이 넣어 주는 공유 저장소입니다.
같은 링크를 연 학생들의 답이 한곳에 모이므로 "우리 반 정답률"이 성립합니다.
`bumpTally(qi, correct)`가 **읽고 → 1 올리고 → 다시 쓰는** 방식으로 갱신합니다.

문제는 그것이 없을 때입니다. 원본은 `localTally`라는 **모듈 변수**로 떨어지므로,
일반 브라우저에서 열면 새로고침과 함께 반 정답률이 0으로 돌아갑니다.
게다가 화면에는 그 사실이 어디에도 표시되지 않아, 교사는 집계가 되고 있다고 믿게 됩니다.

## 6. 원본에서 발견한 문제

| # | 내용 | 결과 |
| --- | --- | --- |
| 1 | `--cyan-2` 변수를 쓰지만 어디에도 정의하지 않음 (139·147줄) | 그라디언트 선언이 무효가 되어 **정답률 막대가 통째로 보이지 않음** |
| 2 | 공유 저장소가 없으면 모듈 변수 `localTally`로 대체 | 새로고침하면 반 정답률이 사라짐 |
| 3 | `hasStore`를 파일 로드 시 한 번만 판단 | 저장소가 늦게 주입되는 환경에서 영영 쓰이지 않음 |
| 4 | 집계 키가 배열 인덱스(`q0`…`q7`) | 문항을 하나만 끼워 넣어도 **이전 집계가 다른 문항에 붙음** |
| 5 | '다시 도전하기'·새로고침 때마다 같은 학생이 다시 집계됨 | `(3/12명)`의 '명'이 사람 수가 아니라 **시도 횟수**. 반 인원보다 큰 수가 나옴 |
| 6 | 5·6번은 '진짜냐'가 아니라 '써도 되느냐'를 묻는데 버튼과 정답 문장은 그대로 | 정답을 맞혀도 **"이건 진짜예요"**라는 엉뚱한 문장이 뜸. 교육용으로는 오히려 오개념 |
| 7 | `escapeHtml(headline).slice(0,26)` — 이스케이프한 뒤 자름 | 엔티티(`&amp;`)가 반 토막 나 깨질 수 있음. 길이 비교(`headline.length`)도 원문 기준이라 서로 어긋남 |
| 8 | 근거 복원값을 `"`만 바꿔 textarea 안에 넣음 (310줄) | textarea 본문에서 정작 escape가 필요한 `<`·`&`는 그대로 들어감 |
| 9 | 진행 막대가 `idx / 8` | 1번을 풀어도 0%, 마지막을 풀어도 87.5%. 막대가 늘 한 칸 뒤처짐 |
| 10 | `.tips` 안에 `<ul>` 없이 `<li>`만 있음 (415~422줄) | 목록이 아닌 마크업. 스크린리더가 항목 수를 읽어 주지 못함 |
| 11 | 해설이 나타날 때 알림이 없음 | 스크린리더 사용자는 정답 여부를 알 수 없음 |
| 12 | `loadTally()`가 `localTally` **객체를 참조로** 돌려주고 `bumpTally`가 그것을 변형 | 읽기 함수가 사실상 쓰기와 얽힘 |
| 13 | 학생이 적은 근거(`reasons`)가 해당 문항 해설에서만 쓰이고 결과 화면에서 사라짐 | 활동의 핵심 산출물이 회수되지 않음 |

## 7. 이식하면서 한 일

원본의 문구·정답·점수 규칙은 그대로 두고, 위 문제만 고쳤습니다.

| 항목 | 원본 | 이 저장소 |
| --- | --- | --- |
| 정답률 막대 색 | 정의되지 않은 변수로 투명 | `--color-cyan-2`를 채워 실제로 보이게 함 |
| 저장소 | `window.storage` 또는 메모리 | `window.storage` → `localStorage` → 메모리 **3단 폴백** (`src/shared/storage.ts`, 방패 앱과 공용) |
| 저장 위치 안내 | 없음 | 결과 화면에 지금 어디에 저장되는지 문장으로 표시 |
| 집계 키 | 배열 인덱스 | 문항 id(`breaking-news` 등). 모양이 달라져 저장 키도 `…_v2`로 올림 |
| 중복 집계 | 재도전할 때마다 누적 | **한 기기는 문항마다 한 번만** 집계 (`deepfake_quiz_counted_v1`). '○명'이 실제 사람 수가 됨 |
| 5·6번 선택지 | 진짜 / 딥페이크 | 문항이 라벨과 정답 문장을 직접 정함 (`써도 돼요 / 안 돼요`, `이건 밝히고 쓴 올바른 사용이에요.`) |
| 상황·해설 문장 | `<b>`가 섞인 HTML 문자열 + `innerHTML` | `{ em }`·`{ quote }` 조각 배열(`RichText`)로 분리해 마크업 주입 경로 제거 |
| 제목 줄이기 | 이스케이프 후 문자 단위 절단 | 코드 포인트 단위로 자름. 이스케이프는 React가 처리 |
| 진행 막대 | 한 칸 뒤처짐 | 답을 고르면 그 문항까지 채워짐 |
| 근거 회수 | 문항 해설에서만 보임 | 결과 화면에 **'내가 적은 근거 되돌아보기'** 추가 (맞고 틀림과 함께 다시 읽음) |
| 반 정답률이 빈 경우 | 아무것도 그리지 않음 | "아직 모인 답이 없어요"라고 밝힘 |
| 접근성 | 해설이 조용히 나타남 | 해설에 `aria-live`, 진행 막대에 `role="progressbar"` |
| 문항 목록 | 배열 인덱스에 의존 | 안정적인 `id`를 갖는 데이터 (`data/questions.ts`) |
| 판별 요령 5줄 | `<ul>` 없는 `<li>` | 정상적인 목록. 강조는 다른 문장과 같은 조각 형식 |

## 8. 이식본의 구조

```
quiz.html                     진입 HTML (Vite 세 번째 엔트리)
src/quiz/
├── main.tsx                  React 진입점
├── quiz.css                  팔레트(@theme) · 배경/카드 그라디언트 · 애니메이션
├── App.tsx                   상태 전부 (문항 번호·선택·점수·근거·집계)
├── types.ts                  Question · Clue · RichText · Tally · AnswerRecord
├── data/questions.ts         사건 8건 (순수 데이터)
├── utils/
│   ├── quizStore.ts          반 정답률 읽고 쓰기 · 첫 도전만 세는 규칙
│   └── grade.ts              점수 → 등급 · 정답률 → 논평 · 저장 위치 문구
└── components/               Rich · ProgressBar · CaseFile · ClueList ·
                              ReasonBox · Choices · ClassBar · Feedback · ResultView
src/shared/storage.ts         공유/로컬/메모리 3단 저장소 (방패 앱과 함께 씀)
```

원본의 `render()`는 사라졌습니다. `index`와 `picked`가 바뀌면 필요한 부분만 다시 그려지고,
해설은 `picked !== null`에서 파생됩니다.

`src/shared/storage.ts`는 원래 `src/shield/utils/storage.ts`였습니다.
퀴즈도 같은 3단 폴백이 필요해 앱 바깥으로 옮기고, 방패 앱은 그 자리에서 가져다 씁니다.

## 9. 남아 있는 한계

- **집계 경합.** 읽고-쓰는 사이의 충돌은 그대로입니다. 두 학생이 동시에 답하면 한 표가 묻힐 수 있습니다.
- **한 기기 = 한 사람.** 중복 집계는 이 기기에 남긴 기록으로만 막으므로, 시크릿 창이나 다른 기기로는 또 셉니다.
- **v1 집계는 이어받지 않습니다.** 키 모양이 달라졌고, 원본의 v1 숫자는 사람 수가 아니라 시도 횟수라 이어 붙일 값이 아니었습니다.
- **점수는 저장되지 않습니다.** 새로고침하면 처음부터입니다. 남는 것은 반 정답률뿐입니다.
- **되돌아가기가 없습니다.** 원본과 같이 앞 문항으로 돌아가 답을 고쳐 볼 수는 없습니다.
- **문항 순서가 고정입니다.** 섞기(shuffle)가 없어, 한 교실에서 두 번 하면 순서를 외웁니다.
