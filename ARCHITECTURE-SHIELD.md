# AI 안전 방패 메이커 코드 분석

분석 대상: 클로드 채팅으로 만든 단일 HTML 파일 `AI 안전 방패 메이커 — 딥페이크 안전 사용 규칙 만들기`
(752줄 · HTML + CSS + 바닐라 JS 한 덩어리)

이 문서는 원본이 **어떻게 작동하고 어떻게 구성되어 있는지**를 정리한 것입니다.
이 저장소의 `src/shield/`는 아래 구조를 React로 옮긴 이식본입니다.

---

## 1. 한 줄 요약

모둠이 딥페이크 상황을 판단하고 → 안전 규칙을 적으면 → 그 문장이 방패 도형에 새겨지고 →
이미지·인쇄·갤러리로 나눈 뒤 → 반 전체 찬반 투표로 마무리하는, **4단계 수업 진행용 단일 페이지 앱**입니다.
서버가 없고 계산도 없습니다. 이 앱이 하는 일은 사실상 **입력한 문장을 그림으로 바꾸고, 그 그림을 나누는 것**입니다.

## 2. 원본의 구성

파일 하나 안에 세 덩어리가 순서대로 들어 있습니다.

| 구간 | 줄 | 내용 |
| --- | --- | --- |
| `<style>` | 7~304 | CSS 변수 팔레트, 4개 패널, 방패 clip-path, 인쇄용 규칙 |
| `<body>` | 306~401 | 헤더 · 단계 표시줄 · `.panel` 4개 · 푸터 (내용은 대부분 비어 있고 JS가 채움) |
| `<script>` | 403~749 | 데이터 상수 3개, 단계 전환, 4개 단계의 렌더·이벤트, 저장소 접근 |

프레임워크도 빌드 도구도 없습니다. 상태는 최상위 `let` 변수 다섯 개이고,
화면 갱신은 해당 영역의 `innerHTML`을 통째로 다시 쓰는 방식입니다.

```js
let judged = {};                 // 상황 id -> "allow" | "deny"
let rules = [...];               // 규칙 5칸
let ruleCount = 5;
let myVote = null;               // "dev" | "reg" | null
let localTally = {dev:0, reg:0}; // 공유 저장소가 없을 때의 대체 집계
```

## 3. 화면 전환 — 패널 4개와 `goStep()`

4개 단계가 모두 DOM에 있고, `.panel.show` 클래스 하나로 보이는 것만 고릅니다.
라우터가 없어 URL은 바뀌지 않습니다.

```js
function goStep(n){
  panels.forEach(p => p.classList.toggle("show", +p.dataset.panel === n));
  navs.forEach(v => { v.classList.toggle("active", s===n); v.classList.toggle("done", s<n); });
  window.scrollTo({top:0, behavior:"smooth"});
  if(n===2){ renderShield(); renderGallery(); }   // 들어갈 때만 다시 그린다
  if(n===3) refreshTally();
}
```

여기서 두 가지가 드러납니다.

- **단계 잠금이 없습니다.** 표시줄의 어느 단계든 눌러 건너뛸 수 있습니다.
  수업 중 앞 단계로 되돌아가 설명하는 일이 잦으므로 의도된 느슨함으로 보입니다.
- **화면 갱신 시점이 수동입니다.** 3단계는 진입할 때와 모둠 이름을 칠 때만 다시 그립니다.
  React로 옮기면 이 `if(n===2)` 분기가 통째로 사라집니다.

## 4. 단계별로 하는 일

### STEP 1 — 상황 판단 (405~476줄)

상황 카드 6장(좋은 쓰임 3 · 나쁜 쓰임 3)에 허용/금지 버튼을 붙입니다. 채점은 없습니다.

**여기에 원본의 가장 큰 구조적 빈틈이 있습니다.** 선택은 `judged[id]`에 저장되지만,
`judged`는 **파일 어디에서도 다시 읽히지 않습니다.** 화면 안내문은
"여기서 정한 마음이 다음 단계의 규칙으로 이어집니다"라고 약속하지만, 코드상 아무것도 이어지지 않습니다.
1단계와 2단계는 서로 모르는 두 화면입니다.

### STEP 2 — 규칙 입력 (478~503줄)

모둠 이름 1칸과 규칙 5칸. 힌트 6개는 `<b>` 태그가 섞인 문자열 배열을 `innerHTML`로 붙입니다.

입력값은 두 경로로 수집됩니다. `input` 이벤트로 즉시 `rules[i]`에 넣고,
단계를 넘어갈 때 `collectRules()`가 DOM을 다시 훑어 `trim()`합니다. 둘은 사실상 중복이지만,
`renderRules()`가 `innerHTML`로 입력칸을 다시 만들기 때문에 값 복원용으로 두 벌이 필요했던 흔적입니다.

### STEP 3 — 방패 (505~689줄) · 이 앱의 핵심

**(1) 모둠 이름 → 도형 선택.** 난수가 아니라 **해시**입니다.

```js
h = (h*31 + s.charCodeAt(i)) >>> 0;      // 문자열을 32비트로 접고
SHIELD_SHAPES[hashName(n) % 5]           // 5종 중 하나
```

같은 모둠 이름은 언제 열어도 같은 방패가 됩니다. 저장 없이도 결과가 재현되므로
"우리 모둠 방패"라는 소유감이 생기고, 갤러리에서 도형만 보고도 모둠을 알아볼 수 있습니다.
이름이 비어 있으면 0번(기사 방패)이 기본값입니다.

**(2) 도형은 같은 모양을 두 벌로 들고 있습니다.**

| 필드 | 쓰임 |
| --- | --- |
| `css` | 화면용 `clip-path: polygon(...)` (백분율) |
| `outer` `inner` `frame` | PNG 저장용 SVG `<polygon points>` (440×528 절대 좌표) |

CSS clip-path는 캔버스로 옮길 수 없어 생긴 이중 표현입니다.
**도형을 고치려면 두 벌을 함께 고쳐야 한다**는 것이 이 앱의 가장 중요한 유지보수 규칙입니다.

**(3) 화면 방패는 div 세 겹**입니다. 금테(`.shield-outer`) 위에 남색 면(`.shield`),
그 위에 안쪽 각인선(`.frame`)을 얹고 셋 모두 같은 clip-path로 자릅니다.
`aspect-ratio:1/1.2`와 `min()` 패딩 덕분에 화면 크기에 따라 통째로 줄어듭니다.

**(4) PNG 저장은 SVG를 거칩니다.**

```
규칙 문자열 → shieldSVG() 문자열 조립 → Blob(image/svg+xml) → objectURL
→ new Image() → canvas(2배 확대, 흰 배경) → toBlob → <a download> 클릭
```

외부 이미지·폰트를 참조하지 않으므로 캔버스가 오염되지 않아 `toBlob()`이 막히지 않습니다.
SVG에는 자동 줄바꿈이 없어 `wrap()`이 20글자마다 직접 자르고, 줄 수만큼 `y`를 내립니다.
첫 줄에만 `◆`를 붙이고 둘째 줄부터 `x`를 92→104로 들여쓰는 것도 손으로 계산한 값입니다.

**(5) 갤러리**는 `window.storage`(클로드 아티팩트 런타임이 넣어 주는 공유 저장소)에
`shield_gallery_v1` 키로 배열을 통째로 저장합니다. 모둠 이름이 같으면 새로 만들지 않고
확인을 받아 교체하므로, 한 모둠이 여러 번 등록해도 카드가 늘어나지 않습니다.

### STEP 4 — 찬반 투표 (691~748줄)

집계는 `{dev, reg}` 두 숫자뿐입니다. 투표 처리가 짧지만 세 가지 경우를 한꺼번에 다룹니다.

```js
const t = await loadTally();                  // 항상 최신 집계를 먼저 읽고
if(myVote && t[myVote] > 0) t[myVote]--;      // 이전 표를 회수한 뒤
if(myVote === v){ myVote = null; }            // 같은 버튼 → 취소
else { t[v]++; myVote = v; }                  // 다른 버튼 → 이동/신규
```

읽고-고치고-쓰는 사이 다른 모둠이 저장하면 표가 하나 사라질 수 있지만(경합),
한 교실 규모에서는 문제되지 않는다고 보고 잠금 없이 둔 설계입니다.
4초마다 `refreshTally()`를 돌려 다른 모둠의 표가 화면에 나타나게 합니다.

논평 문구는 **이긴 쪽을 편들지 않습니다.** '발전'이 많으면 안전장치를 묻고,
'규제'가 많으면 좋은 쓰임을 묻습니다. 토론을 닫지 않으려는 의도가 문구에 박혀 있습니다.

## 5. 원본에서 발견한 문제

| # | 내용 | 결과 |
| --- | --- | --- |
| 1 | `judged`를 어디서도 읽지 않음 | 1단계 활동이 2단계와 끊어짐 (안내문이 빈말이 됨) |
| 2 | `window.storage`가 없으면 전역 변수(`window.__localGallery`, `localTally`)로 대체 | **새로고침하면 갤러리·집계가 사라짐**. 일반 브라우저에서 그대로 열면 저장이 안 됨 |
| 3 | `myVote`를 메모리에만 보관 | 새로고침 뒤 다시 누르면 같은 모둠 표가 **중복 계산**됨 |
| 4 | `setInterval` 등록 조건을 로드 시점에 한 번만 평가 | 공유 토글을 껐다 켜도 자동 갱신이 되살아나지 않음. 반대로 타이머는 영영 멈추지 않음 |
| 5 | 공유 토글을 끄면 `localTally`(0표)로 떨어짐 | 켤 때/끌 때 집계가 서로 이어지지 않아 학생에게는 표가 사라진 것처럼 보임 |
| 6 | `wrap()`이 글자 수만 셈 | 영어 단어 한가운데가 잘림 |
| 7 | 0%인 막대에도 `padding:0 10px` | 표가 0일 때 막대 끝에 글자 조각이 삐져나옴 |
| 8 | `let rules = ["","","","","",""].slice(0,5).map(()=>"")` | 빈 문자열 5개를 만드는 우회 표현 |
| 9 | `a.download`에 모둠 이름을 그대로 사용 | 이름에 `/` `:` 등이 있으면 파일 이름이 깨질 수 있음 |
| 10 | 방패 글자 크기가 고정(`.82rem` / SVG 16px) | **작은 화면에서 긴 규칙 5개를 적으면 방패 아래 뾰족한 부분에서 글자가 잘려 나감**. PNG 저장본도 같은 위험 |

## 6. 이식하면서 한 일

원본의 문구·수치·판정 규칙은 그대로 두고, 위 문제만 고쳤습니다.

| 항목 | 원본 | 이 저장소 |
| --- | --- | --- |
| 1단계 → 2단계 연결 | 없음 | 2단계 상단에 **판단 요약**을 보여 줌 (금지로 고른 상황 / 허용으로 고른 상황을 규칙 쓰기 재료로 되돌려 줌) |
| 저장소 | `window.storage` 또는 메모리 | `window.storage` → `localStorage` → 메모리 **3단 폴백** (`utils/storage.ts`) |
| 내 표 기억 | 메모리 | 저장소에 기록해 새로고침 후 중복 투표 방지 |
| 공유 토글 끔 | 0표 메모리 집계 | `:local` 접미사 키에 **이 기기 전용 집계**를 따로 유지 |
| 자동 갱신 | 로드 시 한 번 판단, 정지 없음 | 4단계에 머무는 동안만 4초 폴링, 벗어나면 해제 |
| 저장 위치 안내 | 없음 | 지금 어디에 저장되는지 화면에 문장으로 표시 |
| 줄바꿈 | 20글자 기계적 절단 | 공백이 있으면 낱말 경계에서 끊음 |
| 0표 막대 | 글자 조각이 삐져나옴 | 막대가 짧으면 표 수를 막대 밖에 표시 |
| 파일 이름 | 모둠 이름 그대로 | 경로·금지 문자 제거 후 사용 |
| 글자 넘침 | 고정 크기, 잘림 | 도형마다 **안전 영역**(`safeRatio`)을 정의하고, 규칙이 그 안에 들어올 때까지 글자 크기를 낮춰 맞춤 (화면은 측정 기반, SVG는 크기를 낮추며 재줄바꿈) |
| 좁은 화면 | 세로비 1:1.2 고정 | 작은 화면에서만 1:1.45로 늘려 규칙 5개가 읽을 만한 크기로 들어가게 함 |
| 힌트 문자열 | `innerHTML`로 `<b>` 삽입 | `{emoji, lead, rest}` 데이터로 분리해 마크업 주입 제거 |
| 이스케이프 | `escapeHtml()`을 직접 호출 | React가 기본 처리 (SVG 문자열 조립에만 이스케이프 유지) |

## 7. 이식본의 구조

```
shield.html                   진입 HTML (Vite 두 번째 엔트리)
src/shield/
├── main.tsx                  React 진입점
├── shield.css                팔레트(@theme) · 양피지/방패 그라디언트 · 인쇄 규칙
├── App.tsx                   상태 전부 + 저장소 호출 (단계 전환·투표·갤러리)
├── types.ts                  Situation · ShieldShape · GalleryEntry · Tally
├── data/                     situations.ts · hints.ts · shapes.ts (순수 데이터 · 도형은 safeRatio 포함)
├── utils/
│   ├── shape.ts              이름 해시 → 도형 선택
│   ├── shieldSvg.ts          SVG 조립 · 줄바꿈 · PNG 저장
│   ├── storage.ts            공유/로컬/메모리 3단 저장소
│   ├── classStore.ts         갤러리·집계·내 표 읽고 쓰기 (키 상수)
│   └── voteText.ts           집계 → 막대 비율 + 논평 문구
└── components/               Panel · StepsNav · Button · SituationStep ·
                              RulesStep · ShieldStep · ShieldPreview · Gallery · VoteStep
```

원본의 `goStep()` 안에 있던 수동 렌더 호출은 사라졌습니다. `step`이 바뀌면 해당 패널만 마운트되고,
방패는 `teamName`·`rules`에서 파생되어 자동으로 다시 그려집니다.

`window.storage` 키(`shield_gallery_v1`, `shield_vote_tally_v1`)는 원본 그대로 유지했으므로,
아티팩트 런타임에서 열면 원본이 남긴 데이터를 그대로 이어 읽습니다.

## 8. 남아 있는 한계

- **집계 경합.** 읽고-쓰는 사이의 충돌은 그대로입니다. 원자적 증감이 필요한 규모라면 서버가 있어야 합니다.
- **한 기기 = 한 표.** 표 중복은 저장소에 기록된 '내 표'로만 막으므로, 시크릿 창이나 다른 기기로는 또 던질 수 있습니다. 교실 활동이라는 전제 위의 설계입니다.
- **규칙 5칸 고정.** `RULE_SLOTS` 상수를 바꾸면 늘릴 수 있지만, 방패와 SVG는 5개까지만 그립니다(`slice(0,5)`).
- **SVG 텍스트 줄바꿈은 글자 수 기준.** 문자 폭을 재지 않으므로 한글·영문이 섞이면 줄 길이가 들쭉날쭉합니다.
- **안전 영역은 눈으로 정한 값입니다.** `safeRatio`는 도형별 실측이 아니라 여유 있게 잡은 상수라, 도형을 새로 추가하면 함께 정해 줘야 합니다.
- **이모지 문장(crest)은 PNG에서 시스템 글꼴에 의존**합니다. 컬러 이모지 글꼴이 없는 환경에서는 빈 네모로 저장됩니다.
- **판단(1단계) 자체는 여전히 저장되지 않습니다.** 새로고침하면 사라집니다. 방패에 새겨지는 것은 규칙뿐입니다.
